# flow-strip-migrate
Automated migration CLI — replaces babel-plugin-transform-flow-strip-types with the maintained fork and optionally strips Flow annotations from your entire codebase. Run: npx flow-strip-migrate .
